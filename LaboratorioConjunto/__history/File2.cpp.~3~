#include<iostream>
#include "Conjunto_Lista.h"
using namespace std;
int main(){

  Conjunto* A=new Conjunto();
  Conjunto* B=new Conjunto();

  A->Insertar(2);
   A->Insertar(3);
	A->Insertar(4);
	 A->Insertar(1);

	 B->Insertar(1);
   B->Insertar(2);
	B->Insertar(3);
	 B->Insertar(4);
	  B->Insertar(5);

	  cout<<"Conjunto A"<<A->to_Str()<<"\n";
	  cout<<"Conjunto A"<<B->to_Str()<<"\n";

	  system("PAUSE");
      return 0;

}

